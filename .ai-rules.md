# プロジェクト基本ルール (AI開発アシスタント向け)

このファイルは、本プロジェクトを編集するAIアシスタントが**必ず遵守すべき基本ルール**をまとめたものです。
編集や新機能追加の前に、必ずこのルールを確認し、逸脱しないようにしてください。

## 1. デザイン・グラフィックに関するルール
*   **リッチな印象の維持**: 
    床や障害物、新しい足場などの3Dオブジェクトを作成する際は、**絶対に単色（`color`プロパティのみのシンプルなマテリアルなど）で済ませないでください。**
    必ず既存のテクスチャー（`assets/` 内の画像）を利用し、必要に応じてUVマッピングを調整して、リッチな3Dの印象を維持してください。

## 2. 難易度・ターゲット層に関するルール
*   **ターゲット層**: 本ゲームは**小学1年生**を対象としています。
*   **アクションの難易度**: 動く床のスピードや、ジャンプの判定、落下時のペナルティなどは「優しく・簡単に」を心がけてください。（理不尽なゲームオーバーは避ける）
*   **レベルデザインの進行**: **最初のステージ（ステージ1）はチュートリアル的な位置づけとし、絶対に「ジャンプしなくてもクリアできる」平坦な設計**にしてください。ステージが進むにつれて段階的にジャンプや動く床を追加してください。
*   **算数の難易度**: 1学期レベル（10以下の足し算・引き算）〜2学期レベル（20以下の繰り上がり・繰り下がり）の範囲内で出題されるようにしてください。
*   **UI/テキスト**: 漢字には極力ふりがな（`<ruby>`タグ）を振り、直感的にわかるUIを維持してください。

## 3. レベル構築・オブジェクト密度に関するルール
*   **リッチな空間の構築**: ダンジョン内がスカスカにならないよう、**クリスタルやモンスター、障害物（壁や柱など）を画面内に豊富に配置**して、賑やかで探索しがいのある空間にしてください。
*   **報酬の配置**: クリスタルは少なくとも1ステージあたり10個以上配置し、「なかなか見つからない」「集める楽しさがない」という事態を防いでください。

## 4. 品質保証に関するルール
*   **動作の自己完結テスト**: ゲームとして成立しなくなるような致命的なバグ（敵が出ない、クリスタルが取れない、足場が渡れない等）がないか、提案前にロジックを必ず再確認してください。

## 5. テクスチャ読み込みとPBRマテリアルに関するルール
*   **非同期クローンによる黒化の防止**: 
    Three.jsでテクスチャを複製（`.clone()`）してリピート率などを調整する際は、**必ず元のテクスチャの読み込みが完全に完了している状態**にしてください。ロード完了前にクローンすると、画像データ（`image` / `source`）が空のままコピーされ、マテリアルが真っ黒になります。
*   **テクスチャのプレロード徹底**: 
    新規にテクスチャアセットやモデルの追加を行う場合は、必ず `main.js` 内の `init()` 関数に存在する `texturesToPreload` リストにパスを登録し、ゲーム起動時に非同期で事前読み込みが完了していることを保証してください。
*   **設定プロパティ参照の確認**: 
    `levels.json` などの設定ファイルからテクスチャパスを参照する際は、階層構造（例: `config.textures.floor`）を正しく指定してください。キーの間違いにより `undefined` をテクスチャパスとして読み込もうとすると、警告エラーとともに足場がすべて真っ黒になります。

## 6. キャッシュバリア（キャッシュバスター）に関するルール
*   **静的アセットのキャッシュ対策**:
    `index.html` に記述されている CSS (`style.css`) や JS (`main.js`) などの静的ファイルを編集・更新する際は、必ず読み込み元のクエリパラメータのバージョン文字列（例: `href="style.css?v1"` → `href="style.css?v2"` など）を更新してください。これを行わないと、モバイル端末（iOS Safari / Chrome）の強力なキャッシュにより、ユーザー側に最新の変更内容が反映されなくなります。
*   **動的設定ファイルのキャッシュ完全回避**:
    `levels.json` や `voice_config.json` などのJSON設定ファイルを非同期取得（`fetch`）する際は、必ずURLの末尾に動的に現在時刻のタイムスタンプ（例: `?cb=Date.now()`）を付与する仕組みを維持してください。これにより、ゲーム実行時に常にサーバー上の最新の設定情報が確実に読み込まれます。

## 7. 音声システム（Web Speech API & VOICEVOX）に関するルール
*   **iOS Safari特有の非同期音声ロード対策**:
    iOS/Safariでは、ページ読み込み直後のタイミングでWeb Speech APIの `speechSynthesis.getVoices()` を呼び出すと空配列が返ってくるバグがあります。このため、起動時（`init`など）にダミーの取得を行ったり、`onvoiceschanged` イベントリスナーを使って音声リストが非同期ロードされたことを検知して確実に適用する仕組みを維持してください。これを怠ると、スマホでの再生時に最も不自然な標準の古い機械音声に強制フォールバックされてしまいます。
*   **高品質な音声の最優先自動選択**:
    Web Speech APIで日本語を再生する際は、デバイスで最も人間の声に近い高品質なAI音声（PC Edgeでは `Natural/Online` 音声、iOS/Safariでは `Siri` や `Kyoko (Enhanced)` 等）が最優先で自動選択されるよう、`voiceConfig` の優先キーワードによる重み付けソートの仕組みを維持・拡張してください。
*   **ハイブリッドAI音声とフォールバックの保証**:
    VOICEVOXはPC（`localhost:50021`）のみで動作するため、スマホ実機やVOICEVOX未起動環境での実行時は、エラーを出さずに瞬時にWeb Speech APIの高品質音声にシームレスにフォールバックされる堅牢な設計（`isVoicevoxAvailable`による判定）を崩さないでください。


